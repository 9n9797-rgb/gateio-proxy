services:
  gateio-autopilot:
    build: .
    container_name: gateio-autopilot
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
